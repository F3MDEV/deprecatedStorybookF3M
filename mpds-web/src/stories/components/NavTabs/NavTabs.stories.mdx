import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { Props } from '@storybook/addon-docs/blocks';
import NavTabs from './NavTabs';

<Meta title="Components/NavTabs" component={NavTabs}/>

# Nav Tabs

Documentação em falta.

## Component API

<Props />

export const Template = (args) => <NavTabs {...args} />

<Canvas>
  <Story name="NavTabs"
    argTypes={{
      onChangeTabs: { action: 'You have just touched the tab!' },
      onClickToggleDropdown: { action: 'Toggled dropdown!' },
      onClickGoBack: { action: 'Going back!' },
      onClickCreate: { action: 'Created!' },
      onClickSave: { action: 'Saved!' },
      onClickClear: { action: 'Cleared!' },
    }}
    args={{
      tabsIndicatorColor: "primary",
      backButtonDisable: false,
      editControlsRoot: "",
      hiddenEditControls: false,
      arrowToggleDropdownClasses: "",
      toggleDropdownTitle: "Report",
      hrefToggleDropdown: "/",
      backButtonTitle: "Previous"
    }}>
    {Template.bind({})}
   </Story>
</Canvas>
