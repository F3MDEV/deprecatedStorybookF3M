import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { Props } from '@storybook/addon-docs/blocks';
import AddField from './AddField';

<Meta title="Components/AddField" component={AddField}/>

# Add Field

Basic input with an add button.

## Component API

<Props />

export const Template = (args) => <AddField {...args} />

<Canvas>
  <Story name="Normal"
    argTypes={{
        onAddClick: { action: 'Item added!' },
        onChangeTextfield: { action: 'Input changed!' },
    }}
    args={{
        colorOfTheButton: "primary",
        disableButton: false,
    }}>
    {Template.bind({})}
   </Story>
</Canvas>
