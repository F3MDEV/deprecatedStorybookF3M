import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { Props } from '@storybook/addon-docs/blocks';
import BackToTop from './BackToTop';

<Meta title="Components/BackToTop" component={BackToTop}/>

# Slider Arrow

Scroll to the top of the page with this button.

## Component API

<Props />

export const Template = (args) => <BackToTop typeOfThePosition = "static"
classes = ""
timeOfTransition= {1000}
/>

<Canvas>
  <Story name="Normal"
    argTypes={{
        handleClick: { action: 'To the top and far beyond!' }
    }}>
    {Template.bind({})}
   </Story>
</Canvas>
